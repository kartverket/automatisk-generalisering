"""TEMPLATE — not shipped.

The executable companion to `docs/refactor/`. Mirrors the source tree that
03-architecture §7 specifies, so that tree is checkable with `ls` rather than being
prose that drifts. On promotion this directory becomes `src/ag/` unchanged.

Read `../README.md` first.
"""
